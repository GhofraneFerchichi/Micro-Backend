eureka:
  instance:
    hostname: 51.8.240.67  # Updated with the mcommandes external IP
  client:
    serviceUrl:
      defaultZone: http://51.8.240.64:8761/eureka  # Updated Eureka defaultZone

server:
  port: 9009

spring:
  application:
    name: mcommandes
  config:
    import: optional:configserver:http://51.8.240.21:9012  # Updated Config server IP address
    user-url: http://172.171.81.145:9010  # Define the user service URL here
    mproduits-url: http://172.171.82.118:9004  # Define the mproduits service URL here
    mpanier-url: http://51.8.240.131:9005  # Define the mpanier service URL here
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.171.82.162:3306/db?createDatabaseIfNotExist=true  # Updated MySQL server IP address
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update

feign:
  client:
    config:
      default:
        url: ${spring.config.user-url},${spring.config.mproduits-url},${spring.config.mpanier-url}
