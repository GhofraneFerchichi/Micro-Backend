eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://51.8.66.223:8761/eureka  # Assuming the Eureka server is running on this address

server:
  port: 9009
spring:
  application:
    name: mcommandes
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://51.8.66.71:3306/db  # Assuming MySQL is running on this address
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
