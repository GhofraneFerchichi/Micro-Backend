eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://172.173.184.146:8761/eureka  # Assuming the Eureka server is running on this address

server:
  port: 9004
spring:
  application:
    name: mproduits
  cloud:
    config:
      uri: http://172.173.184.146:9012  # Assuming the config server is running on this address
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.173.184.146:3306/db  # Assuming MySQL is running on this address
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
