trigger:
  branches:
    include:
      - master

pool:
  name: Default
  demands:
    - agent.name -equals DESKTOP-47P7H1A

variables:
  dockerRegistryServiceConnection: 'DockerHubConnection'
  imageRepository: 'ghofraneferchichi/microuserr'
  tag: '$(Build.BuildId)'

steps:
  - task: Docker@2
    displayName: Build and Push Docker image
    inputs:
      command: buildAndPush
      dockerfile: '$(Build.SourcesDirectory)/user-microservice/Dockerfile'
      containerRegistry: '$(dockerRegistryServiceConnection)'
      repository: '$(imageRepository)'
      tags: $(tag)

  - script: |
      echo "Listing contents of $(Build.SourcesDirectory)/my-chart/templates"
      dir "$(Build.SourcesDirectory)/my-chart/templates"
    displayName: 'List Directory Contents'

  - task: PowerShell@2
    displayName: Update Deployment YAML for user-microservice
    inputs:
      targetType: 'inline'
      script: |
        Write-Host "Updating Deployment YAML file for user-microservice with new image tag"
        $yamlPath = "$(Build.SourcesDirectory)/my-chart/templates/user-microservice-deployment.yaml"
        Write-Host "YAML Path: $yamlPath"
        $content = Get-Content $yamlPath -Raw
        $content = $content -replace 'image: ghofraneferchichi/microuserr:.+', "image: ghofraneferchichi/microuserr:$(Build.BuildId)"
        $content | Set-Content $yamlPath

  - task: PowerShell@2
    displayName: Commit and push updated Deployment YAML files
    inputs:
      targetType: 'inline'
      script: |
        git config --global user.email "ghofranefer01@gmail.com"
        git config --global user.name "GhofraneFerchichi"
        cd "$(Build.SourcesDirectory)/my-chart/templates"
        git checkout master  # Ensure you are on the correct branch
        git pull origin master  # Pull latest changes
        # Use PowerShell to replace the image tag in the deployment.yaml file
        (Get-Content user-microservice-deployment.yaml) -replace 'image: ghofraneferchichi/microuserr:.+', "image: ghofraneferchichi/microuserr:$(Build.BuildId)" | Set-Content user-microservice-deployment.yaml
        git add user-microservice-deployment.yaml
        git commit -m "Update Docker image tag to $(Build.BuildId) in user-microservice deployment YAML file"
        git push origin master  # Push changes back to the repository
