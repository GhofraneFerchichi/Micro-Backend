eureka:
  instance:
    hostname: 10.0.41.57
  client:
    serviceUrl:
      defaultZone: http://10.0.41.57:8761/eureka

server:
  port: 9004

spring:
  application:
    name: mproduits
  config:
    import: optional:configserver:http://10.0.138.22:9012
    user-url: http://10.0.75.250:9010 # Define the user service URL here
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.0.156.86:3306/db
    username: root
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  entity-scan:
    base-packages: com.example.mpanier.model, com.mproduits.model, com.example.mpanier.model.PanierProduct

feign:
  client:
    config:
      default:
        url: ${spring.config.user-url}

servlet:
  multipart:
    enabled: true
    max-file-size: 10MB
    max-request-size: 10MB
